'use strict';

let assert = require('assert');

describe('products.gfxcard', function(){
  it('should have the same property as the main products constructor', function(){
    assert.equal(hasOwnProperty(tenEightyTi, name), true);
  });
});
